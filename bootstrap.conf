# 0. Set variables for Github landing zone repository

GITHUB_BOT_USER="github-machine-user-email"
GITHUB_BOT_NAME="github-bot-name"
GITHUB_URL="https://github.com/"

# 1. Set landing zone customer short name
CLIENT_SHORT_NAME="client-abc"

# 2. Set GCS region for Terraform state storage bucket
GCS_REGION="us-east1"

# 3. Set landing zone default region and network regions
DEFAULT_REGION="us-east1"
WORKLOAD_NETWORK_REGIONS="us-east1,us-east4,us-central1"

# 4. Set organization and billing info
ORG_ID=$(gcloud organizations list --format='value(ID)')
ORG_DOMAIN=$(gcloud organizations list --format='value(displayName)')
BILL_ID=$(gcloud alpha billing accounts list --format='value(ACCOUNT_ID)' --filter='OPEN=True')

# 5. Set logging and iac info
WORKDIR=${PWD}
LOG_DIR=$WORKDIR/logs
LOG_FILE=$CLIENT_SHORT_NAME-"`date '+%y-%m-%d'`.log"
LOG_FILE_PATH=$LOG_DIR/$LOG_FILE

# 6. Set terraform-specific configuration
TF_DIR=$PARENT_DIR/terraform
TF_PLAN="bootstrap.tfplan"

# 7. Documentation link
DEPLOY_KEY_DOC_LINK="https://docs.github.com/en/developers/overview/managing-deploy-keys#deploy-keys"
